[Unit]
Description=LCD display driver
After=syslog.target

[Service]
Type=forking
ExecStart=/usr/bin/LCDd -c /etc/LCDd.conf
ControlGroup=cpu:/
Restart=on-failure
RestartSec=54s

[Install]
WantedBy=multi-user.target
